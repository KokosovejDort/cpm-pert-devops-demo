apiVersion: v1
kind: Service
metadata:
  name: cpm-pert
  labels:
    app: cpm-pert
spec:
  type: NodePort
  selector:
    app: cpm-pert
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30080
